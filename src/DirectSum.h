#ifndef transabs_DirectSum_h
#define transabs_DirectSum_h

#include <armadillo>

using namespace arma;

extern void DirectSum(vec& q, vec& x, vec& y, vec& z, vec& fx, vec& fy, vec& fz, vec& phi, double& Utot);

#endif